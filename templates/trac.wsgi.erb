import sys
sys.stdout = sys.stderr

import os
os.environ['TRAC_ENV'] = '<%= @envpath %>'
os.environ['PYTHON_EGG_CACHE'] = '<%= @envpath %>/eggs'

import trac.web.main

application = trac.web.main.dispatch_request
